Extra visualizations (optional)

These scripts generate additional figures in different languages. They read the
CSV outputs produced by the Python pipeline under `output/processed/` and write
PNGs into `output/figures/`.

Notes
-----
- The Python pipeline already produces the required course figures.
- The scripts below are meant as optional additions to demonstrate multi-language
  plotting and deeper diagnostics.


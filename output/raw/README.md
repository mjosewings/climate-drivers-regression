# Raw data

Collected output from `data_collection.py`:

- **raw_merged.csv** — Merged monthly series from NOAA GML, NASA GISS, NCEI, and OWID (≥1000 rows).

Run `python -m src.data_collection` to refresh.

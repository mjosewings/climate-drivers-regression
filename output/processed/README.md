# Processed data

Pipeline outputs (CSVs):

- **climate_features.csv** — Engineered feature matrix and target.
- **feature_importance.csv** — Feature rankings (linear, RF, GB, permutation).
- **model_predictions.csv** — Actual vs predicted temperature for all models.

Run `python -m src.pipeline` to regenerate.
